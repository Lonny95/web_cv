import * as React from "react";
import {footer} from "../../scss/index.scss";

export class Footer extends React.Component {
    render() {
        return <div className={footer}>
        </div>;
    }
}